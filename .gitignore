# Build output
build/
firmware/build/
sdkconfig
sdkconfig.old

# Python
__pycache__/
*.pyc

# Runtime data. Match by extension: renaming the app once left a
# stale differently-named database unignored.
*.db
*.db-journal

# Recorded breath data. These are real physiological recordings from a
# person - keep them out of a public repo. Remove these lines if you want
# sample data shipped with the project.
breath_calibration/
breath_calibration_archive/
*.csv
web/recordings/
